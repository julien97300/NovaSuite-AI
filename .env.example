# Configuration NovaSuite AI
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins

# ===========================================
# CONFIGURATION GÉNÉRALE
# ===========================================

# Nom du projet Docker Compose
COMPOSE_PROJECT_NAME=novasuite

# ===========================================
# INTELLIGENCE ARTIFICIELLE
# ===========================================

# Clé API OpenAI pour NovaCopilot (optionnel mais recommandé)
# Obtenez votre clé sur : https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# ===========================================
# SÉCURITÉ
# ===========================================

# Clé secrète JWT (générez une clé forte en production)
JWT_SECRET=novasuite_jwt_secret_key_2024_secure_token

# Clé secrète OnlyOffice (pour la sécurité des documents)
ONLYOFFICE_JWT_SECRET=novasuite_onlyoffice_jwt_secret

# ===========================================
# BASE DE DONNÉES
# ===========================================

# Configuration PostgreSQL
POSTGRES_DB=novasuite
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin

# ===========================================
# STOCKAGE MINIO
# ===========================================

# Identifiants MinIO (stockage de fichiers)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET=novasuite-files

# ===========================================
# RÉSEAU ET PORTS
# ===========================================

# Port principal de l'application (défaut: 80)
APP_PORT=80

# Port HTTPS (défaut: 443, décommentez si vous utilisez SSL)
# HTTPS_PORT=443

# Port de la base de données (défaut: 5432)
DB_PORT=5432

# Port MinIO (défaut: 9000)
MINIO_PORT=9000

# Port console MinIO (défaut: 9001)
MINIO_CONSOLE_PORT=9001

# Port OnlyOffice (défaut: 8080)
ONLYOFFICE_PORT=8080

# ===========================================
# DÉVELOPPEMENT
# ===========================================

# Mode de développement (development/production)
NODE_ENV=production

# URL CORS pour le développement
CORS_ORIGIN=http://localhost:3000

# ===========================================
# SAUVEGARDE ET MAINTENANCE
# ===========================================

# Répertoire de sauvegarde (optionnel)
BACKUP_DIR=./backups

# Rétention des sauvegardes en jours (défaut: 30)
BACKUP_RETENTION_DAYS=30
